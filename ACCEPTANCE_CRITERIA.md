# MVP 验收条件

> 以下 AC 保留 V1 基线与实际执行记录。新增状态账本、类别、旧库升级和备份兼容的验收条件见 [V2_CHANGE_SPEC.md](V2_CHANGE_SPEC.md) 第 7 节。V2 本地与主要真机链路已验收，未决边界见下文。

基线：2026-09-13 用户在本任务给出的需求。仓库原为空，未提供独立 PRD。本文是完整 MVP 的验收清单；Phase 1–6 功能已实施，Phase 6 手机 JSON 文件验证已通过，Phase 7 的主屏幕独立启动与离线读取也已在 2026-09-14 补验通过，但安装态离线写入、手机后续更新和 Phase 8 综合验收尚未全部完成。模型以 DATA_MODEL.md 为准，页面以 UI_STRUCTURE.md 为准，执行阶段见 IMPLEMENTATION_PLAN.md。

每条记录执行日期、构建版本、设备/浏览器、结果（通过/失败/受阻）及证据。AC 前缀供实施计划引用；全部通过才能宣告 MVP 验收。桌面自动化不能代替手机安装、文件保存和离线冷启动真机测试。

## AC-A：资产 CRUD（Phase 2）

- AC-A01：Given 空库，When 输入合法名称、购买金额和日期保存，Then 创建唯一 UUID 资产，默认 day、次数 0、空可选项为 null，进入详情；刷新后字段一致。
- AC-A02：Given 已有资产，When 编辑名称、金额、备注或有效日期，Then ID/createdAt 不变，updatedAt 更新，首页与详情重新计算。
- AC-A03：Given 同名资产，When 新增同名或低于 ¥1000 的资产，Then 允许保存；低价仅提示。购买价为 0 允许。
- AC-A04：Given use 模式已有次数，When 切换 day 再切回 use，Then 次数、流水和日期保持不变。
- AC-A05：Given 旧编辑表单，When 另一标签先修改该资产，再保存旧表单，Then 显示冲突且保留草稿，不覆盖新值；重新载入前提示草稿将被替换。

## AC-R：两种投入和收益 CRUD（Phase 3）

- AC-R01：Given 已有资产，When 分别新增 additional、consumable、收益记录，Then 金额、日期、备注保存在对应表并关联正确资产，刷新后不丢失。
- AC-R02：Given 已有流水，When 修改金额/日期/备注，Then 原 ID/createdAt 保留，摘要重算；additional 与 consumable 可切换，收益不能通过编辑转成成本。
- AC-R03：Given 收益为零条，Then 收益汇总为 0，无额外开关；新增收益只扣减净投入，不改变总投入。
- AC-R04：Given 不存在父资产或路由父子 ID 不匹配，When 读取/保存流水，Then 拒绝，不生成孤儿或移动记录。

## AC-M：金额与公式（Phase 1、3、4）

- AC-M01：Given 购买 ¥1000、本体追加 ¥200、耗材 ¥300、收益 ¥100，Then 总投入 ¥1500、净投入 ¥1400；编辑或删除任一金额后按同一公式重算。
- AC-M02：Given 0.01 元合法输入，Then 存为整数 1 分；0.1 元存 10 分；不出现浮点尾数。金额格式化两位小数。
- AC-M03：Given 净投入 100 分、3 次，Then 显示 ¥0.33/次；1 分、2 次显示 ¥0.01/次；-1 分、2 次显示 -¥0.01/次。舍入只用于展示，零不带负号。
- AC-M04：Given 投入 ¥100、收益 ¥150、使用 2 次，Then 净投入 -¥50、次均 -¥25.00，并文字说明收益超过投入，不截为 0。
- AC-M05：Given 多个资产且分母不同，Then 首页仅汇总总投入/收益/净投入，不相加日均或次均。数据库与 JSON 不保存派生 totals 或均值。

## AC-D：按天和日期（Phase 1、4）

- AC-D01：Given 2026-09-13 购买、净投入 ¥1400，When today 同日，Then 持有 1 天、日均 ¥1400；次日为 2 天、¥700。
- AC-D02：Given 合法闰日或跨夏令时日期，Then 以公历自然日计算，不因一天有 23/25 小时偏差；2026-02-29 等非法日期拒绝。
- AC-D03：Given 打开页面跨午夜或后台次日恢复，Then 持有天数、日均、到期状态自动更新，无需改记录。
- AC-D04：Given 购买日期编辑为更早合法日期，Then 立即重算；若晚于任一已有流水或到期日则拒绝并给出限制日期，不改关联记录。
- AC-D05：Given 系统时钟回拨使原购买日期成为未来，Then 读取时提示时钟异常、持有天数取 1，不修改原数据；新增/导入未来业务日期仍拒绝。

## AC-U：按次与 +1（Phase 5）

- AC-U01：Given use 资产次数 0，Then 次均显示“— / 暂无使用记录”，不显示 Infinity/NaN/虚假零均值。
- AC-U02：Given 次数 0、净投入 ¥100，When 点击一次“+ 使用一次”，Then 无确认框或跳页，事务成功后次数 1、次均 ¥100；再次成功点击为 2、¥50。
- AC-U03：Given 卡片按钮，When 点击 +1，Then 不触发详情导航；提交中点击不排队，失败次数不变且提示重试。
- AC-U04：Given 同源两个标签，When 各成功 +1，Then 共增加 2；普通资产旧表单不得覆盖新次数。用真实浏览器 IndexedDB 验证。
- AC-U05：Given 误记次数，When 详情更正为合法绝对整数，Then 保存并重算；原次数已变化则拒绝覆盖。允许更正为 0。
- AC-U06：Given 2,147,483,647 次，Then 禁止 +1；负数、小数次数拒绝。按天不显示 +1；事务发现已删除或变为 day 时拒绝。

## AC-E：到期日（Phase 1、4）

- AC-E01：Given expiryDate=null，Then 显示未设置；小于购买日的到期日拒绝。
- AC-E02：Given 到期日 2026-09-15，When today 为 09-13/09-15/09-16，Then 分别显示距到期 2 天/今日到期/已到期 1 天。
- AC-E03：Given 已到期，Then 持有天数继续增长、仍可 CRUD 和 +1；到期不停止计费或自动报废，无推送任务。

## AC-P：持久化与存储（Phase 2、6、8）

- AC-P01：Given 已提交资产及流水，When 同源刷新、关闭浏览器再打开，Then 三表及次数一致；未提交操作不声称已保存。
- AC-P02：Given IndexedDB 打开失败/禁止存储，Then 显示错误和重试，不回退临时内存假装成功，不自动删库。
- AC-P03：Given 配额不足或事务故障，Then 操作失败且旧数据保持不变，不展示成功。
- AC-P04：When 申请持久存储，Then 分别处理 granted/denied/unsupported，明确仍需 JSON 备份。更换 origin 或浏览器须手动导入迁移。

## AC-B：JSON 备份（Phase 6）

- AC-B01：Given 非空三表，When 下载 JSON，再改库后导入该文件，Then 三表全部字段、ID、时间戳与原快照一致，不合并残留旧记录。
- AC-B02：Given 导出时有并发写，Then 文件来自一致的只读事务快照，不出现孤儿。用实际下载文件重新选择导入测试。
- AC-B03：Given 损坏 JSON、错误根类型、未知版本/字段、缺字段、重复 ID、孤儿、非法枚举/金额/日期/文本，Then 在写库前拒绝并给错误路径，旧库逐字段不变。
- AC-B04：Given 文件大于 80 MiB 或总记录超过 5,000，Then 拒绝；有效最大长度备注（含转义字符）与容量上限 fixture 可完整往返。5,000 条时新增拒绝，编辑/删除/+1 仍可。
- AC-B05：Given 合法文件，When 预览后取消，Then 不写库；确认前显示条数、覆盖全部数据及其他标签写入可能被覆盖的说明，提供先导出入口。
- AC-B06：Given 合法空数组备份，When 明确确认清空替换，Then 三表清空；空文本文件拒绝。
- AC-B07：Given 替换事务 clear 后或 bulkAdd 中途发生异常/配额失败，Then 三表全部回滚，旧库与导入前完全一致；不能只验证资产表。
- AC-B08：Given 其他标签仍开着旧表单，When 导入替换后再提交旧修改，Then 快照冲突或父不存在时拒绝；不声称导入会合并其他标签数据。
- AC-B09：Given 手机系统文件界面，When 导出，Then 文件实际可找到并可重新选取恢复；仅触发下载不算通过。

## AC-DEL：删除（Phase 2、3、8）

- AC-DEL01：Given 有两类流水的资产，When 删除确认，Then 显示资产名、关联条数、不可撤销；取消不变；确认后一个事务删除三表关联数据，其他资产不受影响。
- AC-DEL02：Given 确认框打开后关联流水发生变化，Then 重新显示当前信息并要求再次确认，不用过期条数静默删除。
- AC-DEL03：Given 单条流水，When 删除，Then 只删该条并重算；事务失败不丢数据，过期快照不直接删除。

## AC-X：异常输入（Phase 1–3、6）

- AC-X01：空名称、超过 100 code points 名称、超过 2000 code points 备注拒绝；首尾 trim，空备注变 null，HTML 当文本显示；未配对 surrogate 拒绝。
- AC-X02：金额空串、负数、1.005、1e3、逗号/货币符号、前导零及超限拒绝；购买价可为 0，投入/收益必须大于 0；JSON 不接受字符串金额或小数分。
- AC-X03：未来购买/流水、早于购买的流水、非法日期、非整数/越界次数拒绝。字段错误就地反馈并聚焦首错，其他输入保留。

## AC-W：PWA（Phase 7、8）

- AC-W01：首次交付使用已明确允许的安全源和稳定 origin；真实目标手机可安装、manifest 图标/启动范围正确。普通手机局域网 HTTP 或 file:// 不算通过。
- AC-W02：Given production build 已缓存就绪，When 断网并冷启动，Then 首页/详情深链接可用，CRUD/+1/导入导出不依赖网络；未缓存首次离线不能声称可运行。
- AC-W03：Given 新构建，Then 提示更新，编辑中不自动 reload；保存后接受更新，原库保留，缓存切换成功。旧标签阻塞 DB 升级时提示关闭/刷新，不删库。
- AC-W04：资源全部本地打包，无 CDN、远程字体、业务 API；业务数据不存入 Service Worker 缓存。
- AC-W05：记录真实手机型号、系统/浏览器、安装与浏览器态存储行为和测试结果。安全源方案已确定为临时局域网 HTTPS，真机结果未得到则标待验收，不将桌面 localhost 测试替代手机通过。

## AC-UI：手机与导航（Phase 4、8）

- AC-UI01：320/375/390/430 CSS px 下无页面横向滚动，长名称/备注换行；主操作至少 44×44 CSS px，键盘和底部安全区不永久遮挡保存。
- AC-UI02：首页、资产表单、详情、流水表单、设置导航及浏览器返回正常；不存在 ID/未知路径提供安全返回，不生成假数据。
- AC-UI03：空资产、无流水、加载中、加载错误分别展示；按天/按次主指标与 +1 差异符合 UI_STRUCTURE.md。
- AC-UI04：有未保存修改时应用内导航/后退提示确认，取消保留草稿；成功保存正常跳转。关闭/系统强杀不承诺草稿恢复。
- AC-UI05：输入有 label，错误可关联，确认框管理焦点；成功/失败用文本及 aria-live，不能只靠颜色。

## Phase 6 执行记录（2026-09-13）

桌面 Chrome 无头测试 `npm run test:e2e:backup` 已通过：实际下载 JSON 文件，修改三表后取消预览不写库，再从该文件重新选取并恢复资产、投入、收益的全部字段。`npm test` 覆盖 5,000 条与最长转义备注往返、无效格式/字段/外键/金额/日期、三表事务回滚及并发导出一致性。2026-09-14 小米 15 Pro Android Chrome 从系统文件找到刚导出的 JSON，并重新选取该文件恢复，资产和使用次数正确；AC-B09 的手机文件链路通过。

## Phase 7 执行记录（2026-09-13 至 2026-09-14）

- 构建与桌面：`npm run build` 生成 manifest、192/512/maskable 图标及 Service Worker，无 CDN；`npm run test:e2e:pwa` 验证桌面 Chrome 的 v1→v2 手动更新、未保存表单保护、旧 IndexedDB 保留，以及关闭静态服务后的离线冷启动、CRUD/+1/JSON 文件链路。桌面证据不能代替手机安装验收。
- 手机安全源：目标为小米 15 Pro 的 Android Chrome，Android/Chrome 具体版本未记录。用户允许临时局域网 HTTPS。首次将 HTTP 发到 8443 端口出现 `ERR_EMPTY_RESPONSE`；改用 HTTPS 后通过隐私警告进入，但未安装测试 CA，离线缓存注册失败。安装本次测试 CA 并重启 Chrome 后，隐私警告消失，设置页显示“可离线打开”。
- 手机网址模式：用户关闭 Wi‑Fi 和移动数据、完全关闭 Chrome，再打开同一完整 HTTPS 网址，首页离线启动成功；离线新建按次资产并点一次“+ 使用一次”，重开后资产与次数保留。随后从系统“文件/下载”找到导出的 JSON，重新选取该文件导入恢复，资产与次数正确。AC-W02 的这些网址模式子项和 AC-B09 通过；未测试的其他离线页面/异常情形仍按各自 AC 保留。
- 主屏幕安装：小米 15 Pro 的 Android Chrome 首次尝试时长期显示“正在安装”，未出现图标；2026-09-14 用户随后确认主屏幕应用图标成功出现，从图标以无地址栏独立窗口启动，原资产/次数/流水与浏览器态一致，断网彻底关闭后仍能从图标冷启动并读取同一数据。桌面 Chrome `Page.getAppManifest` 与 `Page.getInstallabilityErrors` 均无错误。此前关于 WebAPK 与局域网不兼容的推断没有得到证实，不作为根因。AC-W01 的真机安装/独立启动子项与 AC-W05 的存储一致性/离线读取子项通过；安装态离线写入和手机端后续版本更新仍未单独测试。

## V2 本地执行记录（2026-09-14）

V2 的 Dexie v1→v2 迁移、v1/v2 JSON 四表校验与回滚、类别管理、状态/结束日期、冻结计费天数、三个状态账本及类别账本均已实现；真机反馈后追加“卖价必填且状态/出售收益原子保存”。`npm run typecheck`、`npm test`（49 项）、`npm run build` 和三项桌面 Chrome E2E 通过。小米 15 Pro（HyperOS `3.0.308.0.WOBCNXM.C11`、Chrome `152.0.7977.82`）的同源 V2 升级、旧数据保留、状态/类别操作、网址模式断网重开与离线写入、隔离地址的 v1/v2 文件恢复均通过，证据及文件名见 V2_CHANGE_SPEC.md 第 9 节。主屏幕独立窗口、原数据共享和安装态断网冷启动读取亦已通过；独立 Android 版本号、旧标签阻塞升级提示及安装态离线写入仍待核对。

## 设计阶段自检记录

下列记录是设计阶段自检，不代表 MVP 功能验收。Phase 1 已运行领域和数据库单测，页面与业务链路待后续 Phase 验收。

1. 过度设计：移除全库逐次序列化容量检查；不增加后端、全局状态、使用日志或容量表。
2. 重复模型：仅 Asset、CostRecord、RevenueRecord；购买价不重复写流水，派生指标不入库。
3. 抽象：只保留纯 domain 函数和直接 Dexie 数据函数，无 Repository/Service 类层。
4. 可执行性：八阶段均明确模块、任务、完成条件、测试和依赖；共享校验先于 CRUD。
5. 需求覆盖：用户列出的 CRUD、公式、按天/次、+1、到期、收益、备份、持久化、PWA、手机及删除均有 AC 分组；独立 PRD 未提供，日后需差异核对。
6. 模型稳定性：到期语义、金额/日期单位、null、外键删除、JSON 版本和迁移规则已固定；扩展不预建空字段。

未决项为独立 PRD 补充后的差异审查、安装态离线写入、手机端后续更新流程、独立 Android 版本号、旧标签阻塞升级和 Phase 8 未覆盖的综合异常场景。Phase 1–6 的手机 JSON 文件链路、Phase 7 网址模式离线关键链路及主屏幕独立冷启动/数据一致性已获真机验证；其余按 IMPLEMENTATION_PLAN.md 执行。
