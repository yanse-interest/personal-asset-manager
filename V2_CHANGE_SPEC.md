# V2 方案：状态账本与自定义资产类别

状态：V2 Phase 1–5 本地实现/自动化及 Android Chrome 主要真机链路已完成；主屏幕独立窗口、数据共享和安装态离线冷启动已通过，安装态离线写入及旧标签阻塞升级等边界仍待核对。需求来源：2026-09-14 用户新增要求及真机验收时对卖价必填的补充；本文件定义 V2 增量。现有 MVP 的未变规则继续以 TECH_SPEC.md、DATA_MODEL.md、UI_STRUCTURE.md、IMPLEMENTATION_PLAN.md、ACCEPTANCE_CRITERIA.md 为准。

用户已确认：同时需要三个状态账本与可自定义类别；卖出金额沿用现有收益流水。2026-09-14 真机验收中进一步确认：新建已卖出资产或从其他状态转为已卖出时，必须在资产表单填写大于 0 元的卖价，保存时与状态原子写入一条“出售”收益；再次编辑已卖出资产不重复写入。已填写的退役/卖出日期停止按天成本分母；退役日期可留空，留空时按天指标继续计算到今天；类别在设置页统一增删改，删除类别时资产转为“未分类”。

## 1. 产品边界与账本口径

增加三个固定状态账本：**服役中、已退役、已卖出**。每个资产在任一时刻恰属其中一个账本。另增加可自定义类别账本，例如“家电”“数码”；每个资产最多属于一个类别，也可“未分类”。状态与类别是两条独立维度：同一资产可以同时出现在一个状态账本和一个类别账本中，不能把两种账本的合计相加。所有账本均为现有资产和流水的实时视图，不创建 Ledger 表，不复制购买价、投入、收益或使用次数。

三个状态账本及每个类别账本显示资产数、总投入、总收益、净投入和资产列表，均继续使用原公式。总览的全库合计只取每个资产一次；三个状态账本相加等于总览合计，所有类别账本加“未分类”也等于总览合计。类别/状态变化只移动资产的**完整历史金额**到当前账本，不回溯分摊历史月份，不生成转账流水。类别账本默认跨全部状态，并可按三个状态筛选；筛选后的金额只计入匹配资产。状态账本可按类别筛选，含“未分类”。不引入资产组合、多标签、预算、折旧、图表或财务总账。

新建已卖出资产或从其他状态转为已卖出时，用户必须手动输入大于 0 元的卖价；同一保存事务自动新增一条 RevenueRecord，金额为输入卖价、日期为结束日期、备注为“出售”。不新增持久化卖价字段或收益类型。再次编辑已卖出资产不重复生成收益；改回其他状态也不自动删除原收益，用户须自行核对。已有 V2 备份或此前版本中卖出但未记卖价的资产仍可读取/导入并手动补记，以免破坏旧数据。净投入允许负值。

## 2. 数据模型增量（目标版本）

仍用现有数据库名 `large-asset-cost`，Dexie schema 从 1 升至 2；现有三表、ID 和流水外键不变。只增加一张 Category 表和 Asset 的三个字段。字段仍遵守 DATA_MODEL.md 的 UUID v4、LocalDate、Instant、整数分、严格字段/nullable 规则。

| 实体 | 新字段 | 类型与规则 |
| --- | --- | --- |
| Asset | `categoryId` | UUID v4 字符串或 null；null 表示“未分类”，非 null 必须引用 Category.id |
| Asset | `lifecycleStatus` | `'active' \| 'retired' \| 'sold'`；中文分别为服役中/已退役/已卖出，新建默认 active |
| Asset | `endedDate` | LocalDate 或 null；active 必须 null，retired 可为 null，sold 必须为 purchaseDate 至 today（含端点）的真实日期 |
| Category | `id` | UUID v4，主键，不可编辑 |
| Category | `name` | 首尾 trim 后 1–40 Unicode code points；同名（trim 后完全相同）禁止，大小写不自动合并 |
| Category | `createdAt` / `updatedAt` | Instant；改名只更新 Category.updatedAt |

Category 最多 100 条，不计入原有资产+成本+收益 5,000 条限额。类别名称不复制到 Asset 或流水中；“未分类”不是 Category 记录。类别创建、改名、删除是管理操作：新建名称冲突拒绝；改名不改 Asset 和流水；删除前展示关联资产数，确认后单事务将这些 Asset.categoryId 置为 null、更新对应 Asset.updatedAt，再删除 Category，其他资产及全部流水不变。删除资产不删除类别，空类别仍可保留。

状态修改在 Asset 上改 `lifecycleStatus` 和 `endedDate`，更新 `updatedAt`，与普通资产编辑共用完整旧快照冲突检测。转为 sold 时另按上段原子新增一条收益，并预留原 5,000 条限额中的一个流水名额；卖价缺失、非法、容量不足或流水插入失败均回滚状态修改。状态可因更正改回 active，此时 endedDate 清为 null，历史成本/收益/次数保留；没有状态事件日志，不宣称保留曾退役/卖出的轨迹。撤销 sold 状态不自动删除原收益，提醒用户自行检查。`endedDate` 可早于已有流水日期，因为结束后的结算费用或收益仍可补录；新建/编辑 CostRecord 和 RevenueRecord 仍遵守购买日到今天的原日期规则，已退役/已卖出资产也允许补记或纠错。

编辑 purchaseDate 时还必须 `purchaseDate <= endedDate`（若有），原有流水/expiryDate 检查照旧。expiryDate 继续是预计到期日，不被当作结束日期；退役/卖出后保留原值，但页面以真实结束状态优先，不再将预计到期提示当成当前待办。

## 3. 计算与操作边界

金额公式完全不变：

```text
totalCost = purchaseCost + additionalCost + consumableCost
netCost = totalCost - revenue
serviceEnd = lifecycleStatus == active || endedDate == null ? today : endedDate
serviceDays = max(1, calendarOrdinal(serviceEnd) - calendarOrdinal(purchaseDate) + 1)
costPerDay = netCost / serviceDays
costPerUse = usageCount > 0 ? netCost / usageCount : null
```

`serviceDays` 是 V2 展示的“计费天数”：active 随本地今天变化；retired 在填写 endedDate 后冻结，未填写时继续计算到今天；sold 在 endedDate 停止。购买当天结束仍为 1 天。已有 v1 的 `daysOwned` 是未结束状态下的旧观察口径，V2 页面改称“计费天数”，避免把已卖出资产描述为仍在持有。系统时钟回拨使今天早于 purchaseDate 的显示保护仍适用；若今天早于已存 endedDate，也提示设备时钟异常，展示时分母至少为 1，不改库。结束后补录成本/收益会改变分子，但不会让分母重新增长；使用次数更正仍会改变按次指标。

只有 `lifecycleStatus=active && costMode=use` 才显示并允许“+ 使用一次”；事务内重新确认这两个条件，退役/卖出后两个标签的旧按钮也不能继续计次。已结束资产仍可更正历史使用次数和编辑原始记录；当 usageCount=0 仍显示“— / 暂无使用记录”。按天/按次模式切换保留次数。状态、类别、日期、金额、次数和汇总均不持久化派生 totals。

## 4. 数据迁移与 JSON 安全

升级数据库使用 Dexie version(2) 的一次原子 upgrade：在原 assets 每条补 `categoryId:null`、`lifecycleStatus:'active'`、`endedDate:null`；原 id、购买价、日期、次数、expiryDate、备注、createdAt、updatedAt 和两类流水逐字段不变。创建 categories 表，`id` 主键、`name` 唯一索引；不为三个账本建表。迁移不能用清库重建。打开旧版本标签导致 blocked/versionchange 时沿用既有关闭标签提示，不删库。发布前要求用户先从旧版导出 JSON，迁移测试在旧库样本上检查全部字段、数量和公式。

备份导出升级为 `schemaVersion:2`，新增顶层 `categories` 数组，assets 含三个新字段；其余 envelope 与两类流水不变。v2 导入严格校验 Category ID/名称唯一、每个 Asset.categoryId 外键、状态与 endedDate 配对、日期及既有全部规则，校验完成后预览，再在四表单事务中全量替换。v1 文件仍可导入：先按**冻结的 v1 字段契约**完整校验，再在内存中升级为 active / null category / null endedDate、空 categories；预览明确标注“旧版备份，导入后所有资产默认为服役中、未分类，并替换当前四表”，确认后按 v2 验证和四表事务写入。未知版本、非法类别引用、无效状态日期及事务失败不得改动旧库。v1 导入不是合并，会替换现有类别；仍提供先导出当前 v2 数据。文件 80 MiB 上限保留，新增 100 个 40 字符类别与每资产三个短字段不会突破原 5,000 条最坏情况预算，实施测试须复核极限文件大小。

## 5. 页面与导航增量

| 位置 | 展示与操作 |
| --- | --- |
| 首页 `/#/` | 保留全库总览；顶部明显展示三个状态账本入口及资产数，下面列类别账本入口（含“未分类”）与各自资产数。现有全部资产列表可保留，不增加复杂图表。 |
| 状态账本 `/#/ledgers/active`、`/retired`、`/sold` | 标题、资产数、总投入/收益/净投入；按购买日倒序的资产卡；可用单选类别筛选（含全部、未分类）；空状态提示新增资产或返回总览。 |
| 类别账本 `/#/categories/:categoryId`，未分类 `/#/categories/uncategorized` | 类别名、跨状态汇总、三个状态计数与状态筛选、资产卡；类别不存在安全返回总览。 |
| 资产新增/编辑 | 类别选择（未分类为默认）；状态选择及条件显示结束日期，新增默认服役中；新建或转为已卖出时在结束日期下方显示必填卖价，状态与“出售”收益同事务保存。保存后资产自动出现在对应账本。类别管理入口通往设置页。状态切换、结束日期与卖价错误反馈。 |
| 资产详情 | 显示类别、状态、结束日期、计费天数；已结束资产不显示 +1，仍提供补记投入/收益与更正次数。已卖出资产显示出售收益与旧数据补记提示。 |
| 设置 / 数据 | 增加“类别管理”：新增、改名、删除；删除非空类别确认“关联 N 件资产转为未分类，资产和流水保留”。备份预览显示备份版本与类别条数。 |

资产卡与详情仍使用原成本函数；只增加状态/类别标签。三个状态入口固定按服役中、已退役、已卖出排序；类别按名称升序、同名规则保持稳定，未分类始终单独显示。账本间用浅层返回入口，空类别不自动删除。首页和账本不相加不同资产的日均/次均。页面对三种状态使用文字，不只靠颜色。已结束资产的预计到期提示弱化而不删除 expiryDate。

## 6. 实施顺序（本地实施记录）

V1 Phase 1–8 的历史不回写为“全部通过”；主屏幕独立启动虽已于后续真机测试通过，安装态离线写入和手机后续更新仍未测。新功能按以下顺序推进，不新增服务器、账号、状态库或通用 Repository/Service 层。

### V2 Phase 1：迁移与兼容底座

**目标**：旧 IndexedDB 和 v1 JSON 可无损进入 v2，现有页面仍可运行。

**文件/模块**：`src/domain/{types,validation,calculations}.ts`、`src/data/{db,assets,backup}.ts`、相关 `*.test.ts` 与 v1 旧库/备份 fixture。

**任务**：固定 LegacyAssetV1 解析类型和新 Category/Asset v2 类型；实现状态日期、类别外键校验；Dexie version(2) 原子 upgrade；v1/v2 备份内存适配与四表事务替换；在现有资产创建路径补默认新字段，同步更新受影响的测试 fixture。不在此阶段展示新账本。

**完成条件**：旧库逐字段保留原资产/流水并补默认状态、旧 JSON 可预览和恢复、v2 四表往返；非法输入或事务失败不改原库。既有页面 typecheck/build 通过。

**测试**：typecheck、全部单测、build、原有使用次数/备份/PWA E2E；特别测试 v1 数据迁移、重复打开、v1/v2 备份、四表回滚、80 MiB/5,000 条边界。

**依赖**：现有稳定构建和可复现的 v1 测试库/备份 fixture。真实手机数据在 V2 发布前另行导出并确认文件存在，不阻塞本地编码。

### V2 Phase 2：类别管理

**目标**：用户能统一创建、改名和删除类别，并为资产指定类别。

**文件/模块**：`src/data/categories.ts`、`src/pages/{SettingsPage,AssetFormPage}.tsx`、查询模块及类别测试。

**任务**：类别名称去首尾空格和唯一校验；设置页管理类别；资产表单选择已有类别或未分类；保存时在事务内验证类别外键；删除非空类别时先确认，再原子转移到未分类。

**完成条件**：改名后关联仍在，删除类别不删资产/流水，空类别可保留；类别选择刷新后持久，过期表单或并发删除被明确拒绝。

**测试**：typecheck、单测、build、浏览器类别 CRUD；同名、超限、删除中途失败、跨标签修改及外键失效。

**依赖**：V2 Phase 1。

### V2 Phase 3：生命周期状态

**目标**：三个状态与真实结束日期改变可操作性和按天分母，不改变原金额事实。

**文件/模块**：`src/data/assets.ts`、`src/domain/calculations.ts`、`src/pages/{AssetFormPage,AssetDetailPage}.tsx`、`src/components/AssetCard.tsx` 及测试。

**任务**：新增/编辑状态和 endedDate；结束后冻结计费天数；+1 在 UI 和事务内仅允许 active/use；新建或转为已卖出时必填卖价并原子新增收益；状态更正与购买日期限制保持原快照保护。

**完成条件**：状态切换不修改流水；结束日分母固定，结束后补记仅改变分子；旧标签残留的 +1 被事务拒绝，历史次数更正仍可用。

**测试**：typecheck、单测、build、原 +1 E2E 和新状态链路；当天结束、跨天、回到 active、系统时钟回拨、卖价必填/原子回滚/不重复入账、结束后补记。

**依赖**：V2 Phase 1–2。

### V2 Phase 4：状态及类别账本 UI

**目标**：三个固定状态账本与动态类别账本可查看、筛选、导航，汇总互相一致。

**文件/模块**：`src/pages/{DashboardPage,StatusLedgerPage,CategoryLedgerPage}.tsx`、`src/app/routes.tsx`、`src/data/queries.ts`、`src/styles.css` 及测试。

**任务**：一次四表只读快照和纯汇总；三个状态页、类别/未分类页、交叉筛选、空状态/不存在路由；首页显示两组账本入口并保持全库总览。

**完成条件**：状态/类别/流水变化实时刷新；三个状态分区与所有类别加未分类分区分别等于全库合计；移动端导航和卡片主操作清晰。

**测试**：typecheck、单测、build、浏览器账本汇总/筛选/返回链路；320–430 CSS px、长名称、空类别、无效路由。

**依赖**：V2 Phase 1–3。

### V2 Phase 5：备份、升级与手机回归验收

**目标**：真实旧数据升级和新功能在手机网址模式可用，发布有可恢复备份。

**文件/模块**：`src/data/backup.test.ts`、`scripts/test-*-e2e.mjs`、必要的页面错误提示、README、ACCEPTANCE_CRITERIA.md。

**任务**：用真实 v1 格式 IndexedDB 样本和实际 v1/v2 JSON 文件验收；测试四表事务回滚、多标签 blocked、离线新构建更新后数据；发布前要求从手机导出并确认旧版 JSON 文件，随后在 Android Chrome 验证账本、状态、类别与备份恢复。

**完成条件**：本文件 V2 验收条件全部通过，旧库和旧备份可恢复，PWA 交付方式未变。主屏幕安装不是本次数据模型变更的隐含实现任务；其独立启动及离线读取后来已在真机补验通过，剩余安装态边界见第 9 节。

**测试**：typecheck、全部单测、build、全部现有及新增 E2E、Android Chrome 关键链路和旧库升级后断网重开。

**依赖**：V2 Phase 1–4。

## 7. V2 可验证验收条件

- V2-01：Given v1 真实格式数据库，When 打开 v2，Then 每件原资产在“服役中 / 未分类”，原 id、金额、日期、次数、备注、时间戳与流水完全不变；重复打开不重复迁移。
- V2-02：Given v1 JSON，When 选择导入，Then 预览明确显示旧版映射及覆盖四表；取消不改库，确认后全部资产为 active/categoryId null/endedDate null，其他原字段保留。
- V2-03：Given v2 JSON，When 导出再导入，Then 四表所有字段精确往返；孤儿 categoryId、重复类别名、非法状态/结束日期、未知版本在写库前拒绝，旧库不变。
- V2-04：Given 新资产，Then 默认 active、未分类、endedDate null；选择已有类别后只出现在该类别账本与当前状态账本，不出现重复金额事实。
- V2-05：Given 已有关联资产的类别，When 改名，Then 所有资产的类别展示同步变更，资产/流水 ID 与金额不变；When 删除并确认，Then 关联资产成为未分类，资产和流水仍在；取消或事务失败全不变。
- V2-06：Given purchaseDate=09-13，When 09-13 标为 retired/sold，Then 计费天数为 1；When 09-14 标为结束，Then 为 2；次日不再增长。active 仍按今天增长，恢复 active 后又按今天计。
- V2-07：Given 已结束资产，When 点击旧标签残留的 +1，Then 事务拒绝且次数不变；仍可更正历史次数、编辑金额/日期、补记结束后的合理结算流水。
- V2-08：Given 新建或转为 sold，When 卖价缺失、为 0 或非法，Then 状态与流水均不保存；When 填写正数卖价，Then 同事务新增一条“出售”收益，按原公式扣减净投入、允许负值；再次编辑 sold 不重复入账，撤销 sold 不自动删收益。既有 sold/无卖价数据保持可读、可手动补记。
- V2-09：Given 多件资产分属三个状态和多个类别，Then 每件恰属一个状态账本与一个类别/未分类账本；各分区合计各自等于首页全库合计，状态或类别修改后即时转移且旧页不再计入。
- V2-10：Given 不存在类别/状态路由、空类别、空状态账本、并发改名/删除、无效类别/日期，Then 有明确空/错误反馈，不造假资产、不删其他数据、不突破原 5,000 条资产流水限额。
- V2-11：Given 旧版已缓存且有本地数据的 Android Chrome，When 同一 HTTPS origin 更新至 v2，Then升级后原资产/流水保留，四表可导出，断网重开可用；旧标签导致迁移 blocked 时提示关闭标签，不删库。

## 8. 方案自检与风险

- 只增加 Category 一张表和 Asset 三个字段；三个状态账本与类别账本都是查询视图，绝不复制流水或派生金额。
- 结束日期是新的真实生命周期字段，不能复用 expiryDate；按天分母冻结是显式 V2 规则变更，原 totalCost/netCost/按次公式不变。
- 最大风险是已有手机 IndexedDB 的原子迁移及 v1 JSON 兼容；先备份、再在旧库样本验证、最后上线。更换 IP/端口/证书来源可能影响访问，但同源 IndexedDB 保留规则不变。
- Android Chrome 主屏幕安装首次尝试未出现图标，用户随后确认图标已出现；从图标启动的独立窗口、原数据一致及断网冷启动亦获用户实测确认。不因安装过程引入公网或后端。

## 9. V2 本地与真机验收记录（2026-09-14）

- `npm run typecheck`、`npm test`（49 项）、`npm run build` 通过；Dexie v1 格式旧库迁移、重复打开、v1/v2 JSON 校验及四表回滚、100 类别/5,000 条上限、状态日期、账本分区合计、卖价必填/不重复/回滚均有单测。
- `npm run test:e2e:usage`、`npm run test:e2e:backup`、`npm run test:e2e:pwa` 通过。Chrome E2E 覆盖 v2 文件实际下载/恢复、取消导入、类别新增/改名/删除、状态修改、状态和类别账本筛选、PWA 更新及断网冷启动。
- 真机：用户在小米 15 Pro Android Chrome 的原同源 `https://192.168.31.210:8443/` 先确认 v1 备份 `asset-cost-backup-20260914-192211.json`，再升级 V2；确认原资产、次数、流水保留且映射至服役中/未分类。新建临时类别/资产、状态转卖出、计费天数、+1 限制、卖价必填并原子生成一条出售收益、重复编辑不重复、类别改名/删除及数据保留均通过。切断 Wi‑Fi/移动数据且停止服务后，同网址冷启动、账本/收益读取、离线新增 ¥0.01 耗材并重开保留、离线 JSON 导出均通过。
- 隔离端口 `:8444` 的独立浏览器数据空间中，真机导入原 v1 文件，预览映射正确且旧资产/流水恢复；取消 v2 导入不写库，较早 v2 文件恢复类别，最新离线 v2 文件恢复类别删除状态、卖出资产与离线耗材。主站未参与覆盖导入。两件临时资产之后已从主站删除，原资产、次数、流水保持，清理后 v2 备份 `asset-cost-backup-20260914-194311.json` 已在手机文件中确认；隔离服务已关闭。
- 320/375/390/430 CSS px 的首页与详情无横向滚动已用 Chrome 检查；真机键盘遮挡、旧标签阻塞数据库升级的提示仍未单独验证。用户确认主屏幕图标进入无地址栏独立窗口，原资产/次数/流水与浏览器态相同，关闭 Wi‑Fi/移动数据并彻底关闭应用后，从图标仍能冷启动并读取同一数据。安装态离线写入尚未单独测试。设备为小米 15 Pro，HyperOS `3.0.308.0.WOBCNXM.C11`，Chrome `152.0.7977.82`；独立 Android 版本号未记录。V2-11 的同源升级、数据保留和网址/安装态离线读取子项通过，blocked 子项未验证；不得把未决项写成通过。
