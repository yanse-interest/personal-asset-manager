# 久用：代码、架构与可靠性审查

审查日期：2026-09-15。范围为当前工作区 PWA，保留审查开始前已有的界面、图标和 v3 数据改动。本次修复没有升级业务数据库版本、修改用户真实数据库或变更部署地址。

## 结论

当前 React 页面 → 领域校验与计算 → Dexie 事务 → IndexedDB 的结构适合个人离线产品。派生费用不持久化、金额使用整数分、备份版本化、四表原子恢复、显式数据库迁移都是值得保留的设计。现阶段的主要缺口集中在错误恢复、异步表单和多标签协作。本轮已落实修复及针对性回归；不能据此保证零 bug，也不能替代目标手机上的新版验收。

## 已修复问题

| 优先级 | 原问题与影响 | 修复位置与行为 |
| --- | --- | --- |
| P1 | 主题存储值无效或 localStorage 被禁用，会在 React 启动前抛错白屏 | `AppearanceSettings.tsx` 校验存储值、默认回退、读写容错；无法持久化时仍能切换外观 |
| P1 | 编辑好物读取失败后仍呈现空表单，保存可能误走新增分支 | `AssetFormPage.tsx`、`RecordFormPage.tsx` 区分加载失败、缺失和可编辑态，编辑必须持有原快照 |
| P1 | 同一表单组件切换资产或流水参数后，可能沿用前一条草稿和快照 | 按资产、流水及类型为内部表单指定 key，独立初始化草稿；详情页也按资产隔离临时状态 |
| P1 | 保存中关闭导航阻断，且只靠渲染后的 disabled 防重复，会发生重复提交或迟到导航 | 资产、流水及类别写入增加同步 ref 门闩；提交中阻止离开，成功后再放行导航，失败保留草稿 |
| P1 | 次数更正使用实时查询中的最新次数作 expected，旧输入可能覆盖另一标签的 +1 | `AssetDetailPage.tsx` 在打开更正时固定次数快照，提交冲突时拒绝覆盖 |
| P1 | 删除好物仅比较关联流水 ID，无法发现确认期间同一流水的金额或备注变化 | `data/assets.ts` 保存并比较完整流水快照；同一时间戳下的内容变化也拒绝旧删除确认 |
| P1 | 一处接受 SW 更新可能自动刷新其他编辑标签；设置页入口可绕过弹出提示的草稿检查 | `pwa/register.tsx` 接管 reload 回调；其他标签保留草稿并等待主动更新；两个入口统一调用 `updateGuard.ts` |
| P2 | 数据库打开失败仍进入业务页；blocked 解除后提示可能残留；渲染异常无产品级恢复页 | `App.tsx` 启动门禁、成功清除阻塞提示、刷新重试；路由错误页提供返回和备份入口 |
| P2 | 分类分布忽略未分类好物，统计可能空白或数量不全 | `StatsPage.tsx` 加入未分类统计及空态 |
| P2 | 次数更正、类别草稿缺少离开保护；确认框及图标选择器缺少焦点约束 | 增加导航保护、共享模态焦点循环、Escape 关闭、焦点恢复与独立标题 ID |
| P2 | E2E 固定端口与其他项目或测试相撞，出现 EADDRINUSE | 四个测试入口通过 `scripts/test-ports.mjs` 分配临时端口，使用独立临时 Chrome profile |

## 验证范围

最终结果：`npm run verify` 全部通过；TypeScript/生产构建、57 个单元测试和四组 Chrome E2E 通过。首次完整日志：`/tmp/jiuyong-verify-20260915.log`；Vitest 4.1.11 升级后的 Node 24.19.0 完整复验也全部通过，日志为 `/tmp/jiuyong-verify-security-20260915.log`（均为临时文件）。

统一入口：`npm run verify`。Chrome 默认使用 macOS 的 Google Chrome 路径，其他环境通过 `CHROME_PATH` 指定。测试服务器只绑定 `127.0.0.1`，业务数据和故障注入都在临时浏览器 profile 内进行。

- TypeScript 检查和生产构建。
- 全部单元测试：金额/日期/费用、事务与并发、备份校验与回滚、容量上限、图标目录、v1/v2→v3 迁移；新增删除快照与 blocked/versionchange 生命周期回归。
- usage E2E：两个同源标签各 +1、首页即时反馈且不导航。
- backup E2E：实际 JSON 下载/恢复、取消不写入、旧版备份兼容、类别 CRUD、状态和账本。
- PWA E2E：生产缓存、手动更新、跨标签草稿保留、设置类别草稿保护、更新后原库保留、停止服务器并重启 Chrome 后离线 CRUD/+1/JSON 恢复。
- reliability E2E：无效/禁用外观存储、数据库启动失败、编辑读取失败、同步重复提交、切换资产、取消离开保留草稿、跨标签更正冲突、模拟配额写入失败后重试、写事务排队时禁止离开、未分类统计、404 与渲染错误恢复；320/375/390/430 CSS px 主要页面无横向溢出。

## 架构边界与剩余工作

1. **数据持久性。** IndexedDB 是当前浏览器 origin 的数据空间，无法替代独立备份。已有导入导出和持久存储申请；继续保留同地址升级与升级前导出 JSON 的规则。改用账号云同步属于另一个产品决策。
2. **手机验收。** 本轮自动化运行在桌面 Chrome。新版的 Android 安装态离线写入、同源更新、系统文件恢复、键盘及触控操作仍需真机复核。历史旧版通过的项目不自动视为本轮通过。
3. **性能。** 构建仍提示主 JS chunk 超过 500 kB（约 546 kB，gzip 174 kB），总预缓存约 3.54 MiB，主要还包括本地图标。当前离线链路通过；之后若手机测得明显启动延迟，再按测量拆分图标选择器/页面和压缩图片，不为消除警告改动更新体系。
4. **维护性。** SettingsPage 集合类别、备份、外观和 PWA 状态，后续扩展时可按功能拆分组件；业务事务继续集中在 data 层，不增加重复状态或通用 Repository 层。已有部分表单错误还是表单级提示，逐字段关联和完整无障碍读屏验收可继续完善。
5. **依赖审计已完成。** 用户随后明确授权向官方 npm registry 发送依赖名称与版本。首次审计为 1 项 critical、1 项 moderate，均来自开发依赖 Vitest 3.2.4 / @vitest/mocker。已升级到 Vitest 4.1.11，最终全量审计为 **0 项已知漏洞**。生产 PWA 未打包 Vitest；当前测试配置也未开启其 UI/API 或 Browser Mode，因此未发现公告所述服务暴露路径。该结论不等于无未知漏洞。审计结果见 [JSON](audits/npm-audit-2026-09-15.json)。
6. **测试基础设施。** 临时端口是在短暂保留后交给服务器/Chrome，仍存在极小的端口抢占窗口；CDP 测试脚本尚可进一步共用连接/超时清理代码。当前四组可顺序复跑，范围限所列业务与异常场景。

错误边界机制已核对 [React Router 官方文档](https://reactrouter.com/how-to/error-boundary)；SW 更新回调以当前安装的 vite-plugin-pwa 类型定义和客户端实现为依据，并由真实双标签更新验证。

## 依赖审计补充（用户授权后）

- [Vitest UI 文件读取与执行公告](https://github.com/vitest-dev/vitest/security/advisories/GHSA-5xrq-8626-4rwp)：风险条件包括暴露测试 API/UI 服务，或 Windows 下使用相关模式。项目当前为 `vitest run` + Node 环境。
- [Mock redirect 路径穿越公告](https://github.com/vitest-dev/vitest/security/advisories/GHSA-82fw-gwwq-j7x9)：官方明确修复于 4.1.11，3.x 不提供此修复；因此没有采用 npm 初次提示的 3.2.7。
- 修改仅涉及测试依赖与锁文件、Node 支持范围和文档。新增 `.nvmrc` 指向 Node 24；`engines` 与 Vitest/Vite 支持范围一致。未升级业务运行时依赖，未更改全局开发工具。
- 当前机器的 Node 23 不受新版 Vitest 支持；此次使用本机已有 Node 24.19.0。npm 10 依赖解析发生内部错误后，使用临时 npm 12.0.2 完成安装。
