# MVP 页面与交互

本文件只定义信息、导航和交互；模型/校验以 DATA_MODEL.md 为准。手机优先，不设计主题系统、图表或复杂导航。

## 1. 页面及路由

路由由 createHashRouter + RouterProvider 管理，以下 `/` 实际表现为 `/#/`。

| 页面 | 路由 | 入口与返回 |
| --- | --- | --- |
| 首页 / 资产看板 | `/` | 应用入口；顶部新增和设置 |
| 新增资产 | `/assets/new` | 首页；保存到新资产详情，取消回首页 |
| 编辑资产 | `/assets/:assetId/edit` | 详情；保存/取消回详情 |
| 资产详情 | `/assets/:assetId` | 首页卡片；返回首页 |
| 新增投入/收益 | `/assets/:assetId/records/new?type=additional` | 详情三种新增入口；type 可为 additional/consumable/revenue |
| 编辑投入 | `/assets/:assetId/costs/:recordId/edit` | 详情流水行；保存/取消回详情 |
| 编辑收益 | `/assets/:assetId/revenues/:recordId/edit` | 详情流水行；保存/取消回详情 |
| 设置 / 数据 | `/settings` | 首页；返回首页 |

不存在 ID、父子 ID 不匹配显示“记录不存在或已删除”，不泄漏另一资产的编辑入口。未知路径提供返回首页；新增页未知 type 显示类型选择，不猜测成收益。返回优先明确父页面，浏览器后退仍正常；有修改的未保存表单需离开确认，原生关闭保护仅尽力提供。

## 2. 首页 / 资产看板

显示资产数量、总投入、总收益、净投入合计；不显示跨资产总日均或总次均。资产卡片按购买日倒序排列，同日按 ID 稳定排序。每张显示名称、购买日、净投入、主观察指标、到期信息。

按天卡片：`¥x.xx / 天`、持有 N 天。按次卡片：`¥x.xx / 次`、累计 N 次、独立“+ 使用一次”按钮；零次显示“— / 暂无使用记录”。按钮点击不触发卡片导航。负净投入按模型保留负号并文字说明，不只靠颜色。

点卡片去详情。空库显示“记录第一件大件资产”主按钮和“导入已有备份”次入口。加载时显示加载状态；加载失败显示错误与重试，不显示空库引导。V1 不增加搜索/分类/拖动排序。

## 3. 新增 / 编辑资产

字段：名称（必填）、购买金额（必填）、购买日期（必填，新增默认今天）、主要观察方式（日/次）、到期日期（可空）、备注（可空）。新增按次资产另有初始累计次数（默认 0）；普通编辑不包含可覆盖累计次数的输入。

金额使用 `inputMode=decimal` 文本输入，按校验契约解析；次数使用数字键盘并按字符串整数校验。低于 ¥1000 显示轻量建议，不阻止保存。到期日期旁说明“仅用于到期提示，不停止成本计算”。

点击保存先校验，错误定位字段；通过后按钮显示保存中并禁用；事务成功后导航。失败保留草稿；并发快照冲突提示“记录已在其他页面修改，请重新载入后核对”，提供重新载入操作，明确会替换草稿并再次确认。切换模式说明次数会保留。

修改购买日期若与已有流水/到期冲突，显示具体限制日期，不偷偷改关联记录。资产删除只在详情提供，避免普通表单误操作。

## 4. 资产详情

显示名称、购买金额/日期、主要观察方式、备注、持有天数、到期日期和到期状态；成本摘要分列本体、后续本体投入、耗材投入、总投入、收益、净投入。

按天：突出日均；不显示 +1。按次：突出次均、次数和“+ 使用一次”，日均与持有天数作为辅助信息；次数旁提供“更正次数”小表单，输入绝对非负整数、保存/取消，保存时检测原次数冲突。不得通过删除资产重建来纠错；不增加逐次日志或撤销栈。

下方显示按日期降序的投入/收益统一阅读列表，每行显示日期、类型、金额、备注。成本/收益用文字区分，金额本身显示正金额；点击行进入对应编辑页。可选 type 的只读 UI 联合值仅用于渲染，不是第四个持久化模型。无流水显示“暂无后续记录”与三种新增入口。收益为 0 时摘要仍显示 ¥0.00。

主要操作：编辑资产、添加本体投入、添加耗材投入、添加收益、按次 +1、删除资产。到期、超过净投入均不禁用记录和使用操作。

## 5. 新增 / 编辑投入（含收益复用表单）

显示所属资产名称；创建时类型选择“本体投入 / 耗材投入 / 收益”，从入口预选；金额、日期、备注。日期默认今天，金额必填且 >0。不出现耗材名称/库存、维修子分类或收入类别。

编辑成本允许 additional/consumable 切换，编辑收益固定收益；不允许跨资产移动或成本变收益。页面底部保存/取消，已有记录另提供删除。成功返回详情并看到重算结果；失败保留输入。收益提示“从净投入中扣减”。

## 6. + 使用一次

一次有效点击发起一次事务，不弹确认框，不跳页、不要求备注。仅当前资产按钮提交中禁用；成功后响应式刷新次数、次均，短暂显示“已记录 1 次”；失败提示“记录失败，请重试”，次数不变。提示使用 aria-live，不只用动画。

两个标签各成功点击一次应累计 2；重复点按发生在本按钮禁用期间不排队。事务完成后再次点击是另一次有效使用。达到次数上限显示说明并禁止 +1；资产被删除/切为按天时提示刷新，不重新创建记录。误记通过“更正次数”修正，当前 MVP 不做撤销按钮。

## 7. 设置 / 数据导入导出

显示本地数据说明、三类记录数、格式版本、导出 JSON、导入 JSON、存储持久化申请及结果、PWA 可用/离线就绪/更新状态。不要将 denied 显示为数据已丢失，也不宣称 granted 等于备份。说明数据只在当前浏览器数据空间，定期导出可迁移。

导出后提示检查文件保存位置；移动浏览器若下载行为不同，以系统文件保存界面和实际可重新选择为验收标准。

导入依次显示选择文件、验证中、预览（备份时间、三类条数）、覆盖确认、导入中、成功/失败。确认文案包含“替换当前全部资产和流水，其他标签刚写入的数据也可能被覆盖；建议先导出并关闭其他标签”；预览提供导出按钮。空文件内容不是空库备份；合法空数组备份明确提示“将清空当前数据”。取消后原库完全不变。

不另做“一键清空所有数据”，减少危险入口；导入空库仍受覆盖确认。无联网后台、账号或同步状态。安装使用浏览器系统支持能力，有安装事件则提供入口，否则给适用平台的添加到主屏幕说明；没有安全源时明确“当前环境不能启用 PWA 离线安装”，不能伪造已就绪。

## 8. 删除、错误与手机要求

删除资产：确认框显示资产名和关联投入/收益条数，说明全部删除不可撤销，取消为安全默认；确认时事务重新验证实体及关联快照，若新增流水导致确认内容变化则刷新数量再次确认。删除流水：展示类型、日期、金额，确认后只删该条。失败保持页面和原数据。

每个输入有可见 label、错误关联、首错焦点；确认框管理焦点与关闭返回；所有按钮有可访问名称。320–430 CSS px 手机竖屏无页面级横向滚动，长名称/备注换行，主操作触控目标至少 44×44 CSS px，适配底部安全区和软键盘，保存操作不被永久遮挡。桌面限制内容宽度即可。加载/禁用状态不依赖颜色，不把视觉设计扩展成组件库项目。
